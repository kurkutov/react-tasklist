import React, { useContext } from 'react'
import { TaskContext } from '../context/task/TaskContext'

export const New = () => {

    // const context = useContext(TaskContext)

    // context.getTasks("new")

    return (
        <div>
            <h1>New page</h1>
        </div>
    )
}